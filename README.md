# AI Agent Lab

Local-first LLM orchestration simulator. Models a multi-agent pipeline (planner, researcher, coder, reviewer, synthesizer) processing software engineering tasks. Tracks latency, token usage, and quality metrics.

Runs daily via GitHub Actions. Reports in `logs/`.
